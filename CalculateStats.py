import statistics
data = [7,5,4,9,12,45]
print("Mean of the sample is % s and standard deviation is % s" %(statistics.mean(data), statistics.stdev(data)))
#print("Standard Deviation of the sample is % s "%(statistics.stdev(data)))
